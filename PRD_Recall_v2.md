# Recall AI 智能错题本 · 产品需求文档（PRD v2 · 专业版）

> 版本：v2.0（专业重构版，替代 `PRD_Recall.md`）
>  owner：产品经理
> 最后更新：2026-08-13
> 配套文档：`DEV_PLAN.md`（开发规划）、`PRD_UIUX.md`（UI/UX 基线）
> 依据：Vue3 + FastAPI + SQLite + ChromaDB + 多 AI 供应商 + OCR 的实际代码快照（2026-08-13）

---

## 0. 文档定位

本 PRD 用标准 PRD 结构重述 Recall 产品需求，强调 **Why 优先于 What**、明确 **Non-goals** 防范围蔓延、需求用 **MoSCoW** 分级、指标 **可衡量可归因**。所有"已落地/缺口"判定基于真实代码，避免规划与现状脱节。

> ⚠️ 证据说明：问题陈述中的用户痛点来自中学生/备考群体的通用工作流常识与产品假设，**非一手用户研究结论**。正式立项前建议补齐用户访谈（见 §7 Q1）。

---

## 1. 问题陈述（Problem Statement）

学生（尤其初高中、考研/考证群体）在日常练习与考试中持续产生错题，但传统错题本存在结构性低效：

- **录入成本高**：手抄或剪贴题目，单题 3–8 分钟，日均 30–60 分钟耗在"搬运"而非"理解"；
- **归因弱**：多数只记录答案，缺乏系统化的**错因**与**知识点关联**，同类错误反复出现（迁移失败）；
- **复习无计划**：凭感觉复习，未用间隔重复（SM-2）管理遗忘曲线，考前突击效率低、易焦虑。

**受影响人群与频次**：核心用户（初三/高三/考研）几乎每日产生 3–10 道错题；低效直接转化为"错题本坚持不下去"的高弃用率。

**不解决的代价**：
- 用户侧：知识点长期薄弱，成绩提升缓慢；
- 产品侧：若无"录入快 + 复习科学"的闭环，产品沦为又一个"记完就忘"的工具，留存与口碑受损；
- 竞争侧：市面已有拍照答疑类 App，但"个人错题的智能化复习管理"仍是空白，先发体验优势关键。

---

## 2. 目标（Goals）

### 2.1 用户目标（用户得到什么）
| # | 目标 | 可衡量标准（成功信号） |
|---|------|------------------------|
| U1 | 极速录入 | 单题从"想法到归档"≤ 30s（拍照/OCR 场景端到端 < 10s） |
| U2 | 拿到真解析 | 录入后 ≥ 90% 题获得可用 AI 解析（学科/知识点/错因/分步），且单供应商故障不中断 |
| U3 | 科学复习 | 系统基于 SM-2 自动生成到期复习队列，用户无需手动规划 |
| U4 | 闭环巩固 | 复习中能获得同知识点变体题 + 自动批改，定位薄弱点 |

### 2.2 业务目标（公司得到什么）
| # | 目标 | 可衡量标准 |
|---|------|-----------|
| B1 | 留存提升 | 周留存（WAU/注册）≥ 40%（假设基线 25%，目标 +15pt） |
| B2 | 活跃复习 | 北极星指标「周有效复习题数」逐月 +20% |
| B3 | 数据资产 | 人均累计错题 ≥ 200 条（本地知识库沉淀，支撑后续推荐/导出变现） |

> 目标均为**结果（outcome）**而非产出（output）；达成手段见 §5。

---

## 3. 非目标（Non-Goals）

明确本期 **不** 做什么，防止范围蔓延：

| # | 非目标 | 为何不做 |
|---|--------|----------|
| NG1 | **不做多用户/云端账号体系** | 当前"本地优先"是核心卖点且降低隐私门槛；账号体系成本高、与 MVP 价值无关，单列 initiative |
| NG2 | **不做完整题库/组卷/出卷系统** | 聚焦"个人错题管理"，组卷是相邻大功能，会稀释核心闭环 |
| NG3 | **不做移动原生 App** | 仅做响应式 Web（<768 可用即可），原生 App 投入产出比低，待 Web 验证 PMF 后再议 |
| NG4 | **不做 AI 代写/直接给答案的"作业神器"** | 合规与产品价值观风险；定位"辅导"而非"替代思考" |
| NG5 | **不做社交/分享社区（v1）** | 导出（PDF/Markdown）可满足轻量分享，社区是独立增长项目 |

---

## 4. 用户故事（User Stories）

> 格式：`作为 [角色]，我希望 [能力]，以便 [价值]`。按优先级排列，含边界/错误态。

**P0（核心闭环）**
1. 作为备考学生，我希望拍照/粘贴题目后系统自动识别并归档，以便把时间花在弄懂而非抄写上。
   - 边界：图片模糊/无 OCR 依赖时，降级为手动文本录入，不中断流程。
2. 作为备考学生，我希望录入时 AI 自动给出学科、知识点、错因和分步解析，以便立刻理解错在哪。
   - 边界：某供应商 Key 失效时自动切换下一个可用供应商；模型返回非规范文本时保留原文并标注"部分解析"。
3. 作为日常学生，我希望在错题本里按学科/知识点筛选、查看解析、删除，以便管理我的错题。
4. 作为备考学生，我希望打开"复习"就能看到系统排好的待复习题，逐题作答并标记掌握度，以便无痛坚持复习。
5. 作为自学成人，我希望 AI 对话能流式回答我的问题，并一键把题目加入错题本，以便答疑与归档无缝衔接。

**P1（增强体验）**
6. 作为备考学生，我希望系统对一张含多题的图自动拆成独立题卡让我勾选导入，以便一次拍多题。
7. 作为备考学生，我希望复习时系统出同知识点变体题并自动批改，以便验证是否真会。
8. 作为家长，我希望看数据看板了解孩子的薄弱知识点，以便针对性辅导（只读视角）。
9. 作为用户，我希望导出 PDF/Markdown 错题本，以便打印或离线复习。

**P2（未来）**
10. 作为用户，我希望收到复习提醒通知，以便不忘记复习。
11. 作为用户，我希望看到知识图谱（学科—知识点掌握度），以便宏观把握薄弱面。

---

## 5. 需求（Requirements · MoSCoW）

### 5.1 Must-Have（P0，缺一则不成产品）

#### M1 多方式录入
- 支持 文本粘贴 / 截图 / 图片上传 / 对话转错题 四种入口（前端 `EntryModal` + `ChatWindow`）。
- 验收：
  - [ ] Given 用户在首页点「＋录入」, When 选择任一口子, Then 能进入录入流程并成功入库
  - [ ] Given 无 AI Key, When 提交文本, Then 仍入库（标注"未配置 AI"，不白屏）

#### M2 AI 自动解析（多供应商容错）
- 录入时调用可用 AI 识别 学科/知识点/错因/分步解析；单供应商失败自动 fallback；JSON 非法时保留原文（`partial`）。
- 验收：
  - [ ] Given 配置了 ≥1 个可用 Key, When 录入, Then 返回 `ai_status ∈ {ok, partial}` 且三态徽章正确
  - [ ] Given 所有 Key 失效, When 录入, Then 走 `fallback`，错题仍入库，toast 透出可读原因
  - [ ] Given 模型返回非 JSON 文本, When 解析, Then `ai_status=partial`，原文完整保留

#### M3 错题本管理
- 列表展示、按学科/知识点/来源筛选、查看解析（折叠）、删除（带确认）。
- 验收：
  - [ ] Given 有 ≥1 条错题, When 进入错题集, Then 卡片含题目/学科/知识点/来源/复习次数
  - [ ] Given 输入筛选条件, When 筛选, Then 列表实时更新且无整页刷新

#### M4 逐题复习（SM-2 排程）
- 系统基于 SM-2 生成到期复习队列；逐题呈现，标记 掌握/还不太会/跳过；更新间隔与易错度。
- 验收：
  - [ ] Given 存在到期题, When 打开复习, Then 队列数量 = 到期题数，进度条正确
  - [ ] Given 用户标记"掌握", When 提交, Then 该题为 reviewed 且 review_count+1，下次 due 按 SM-2 推后
  - [ ] Given 标记"还不太会", When 提交, Then 保持未掌握、易错度提升、更早进入下一队列
- 技术注意：当前仅"逐题标记"（`/mistakes/{id}/review`），**需新增 `ReviewLog` 模型与排程服务**（缺口，见 §附录）。

#### M5 AI 对话（流式 + 转错题）
- 提问后逐字渲染；可一键转错题并触发解析。
- 验收：
  - [ ] Given 已配置 Key, When 提问, Then 首字 < 2s 流式渲染
  - [ ] Given 对话中含题目, When 点"加入错题本", Then 进入错题本并参与复习闭环

#### M6 设置与连接测试
- 配置多供应商 Key（仅存本地）+ Base URL；每供应商「测试连接」返回 连通/原因/延迟。
- 验收：
  - [ ] Given 用户填 Key, When 点测试连接, Then 显示 ✓XXms 或 ✗可读原因
  - [ ] Given 保存, When 关闭弹窗重开, Then Key/URL 仍在（localStorage 持久化），后端无落库

### 5.2 Should-Have（P1，显著提升体验）

#### S1 图片 OCR 真实识别
- 安装 PaddleOCR-VL（或等价方案），端到端 < 10s；缺依赖时降级手动录入。
- 验收：清晰印刷体识别准确率 ≥ 90%；模糊图给出"识别不清，请手动补充"提示。

#### S2 多题拆分
- 单图多题 AI 拆分为独立卡片 + 勾选导入。

#### S3 变体题 + 自动批改（接入复习流）
- 复习时调 `/ai/variant` + `/ai/grade`，结算报告含正确率与薄弱点。

#### S4 数据看板
- 趋势（录入/复习/正确率）+ 知识图谱可视化（基于 `/analysis/*`）。

#### S5 导出
- PDF（已有 ReportLab）+ **Markdown 导出**（新增）。

#### S6 帮助中心
- FAQ + 故障排查 + Key 配置引导（当前为占位）。

### 5.3 Could-Have（P2，本期不阻塞）

- C1 复习提醒通知（前端本地）
- C2 相似题前端「相关错题」入口（接口已通）
- C3 深色模式
- C4 错题去重（相似度）

---

## 6. 成功指标（Success Metrics）

### 6.1 指标层级
- **北极星**：周有效复习题数（= 一周内被标记掌握/还不太会的去重题数）——直接衡量"科学复习"价值。
- **驱动指标**：录入转化率（打开→入库）、AI 解析成功率（ok+partial / 总录入）、复习完成率（完成队列/到期队列）、变体题使用率。
- **健康指标**：AI fallback 率（应 < 10%）、OCR 失败率、单题 AI 成本、首屏/首字延迟达标率。

### 6.2 目标值与评估

| 指标 | 成功阈值 | 拉伸目标 | 测量方式 | 评估时点 |
|------|----------|----------|----------|----------|
| 单题录入耗时（拍照） | < 10s | < 6s | 前端埋点起止 | 上线 1 个月 |
| AI 解析成功率 | ≥ 90% | ≥ 95% | `ai_status` 统计 | 上线 1 个月 |
| AI fallback 率 | < 10% | < 5% | 后端日志聚合 | 持续 |
| 周有效复习题数（北极星） | 人均 ≥ 15 | ≥ 25 | 复习事件 | 上线 1 季度 |
| 周留存（WAU/注册） | ≥ 40% | ≥ 50% | 本地活跃事件 | 上线 1 季度 |
| 复习完成率 | ≥ 70% | ≥ 85% | 完成/到期 | 持续 |

> 基线说明：当前无生产数据，阈值基于假设与行业基准；上线后 2 周内校准基线（见 §7 Q2）。

---

## 7. 开放问题（Open Questions）

| # | 问题 | 负责人 | 类型 |
|---|------|--------|------|
| Q1 | 用户痛点是否经一手访谈验证？样本量与核心场景？ | 用户研究 / PM | Blocking（立项前） |
| Q2 | 成功指标的基线值从哪来？上线后如何校准？ | 数据 | Non-blocking |
| Q3 | OCR 选 PaddleOCR-VL 本地部署，还是云端 OCR API（成本/隐私权衡）？ | 工程 / PM | Blocking（S1 前） |
| Q4 | 错题数据是否需支持导出到第三方笔记（如腾讯文档）？涉及合规 | 法务 / PM | Non-blocking |
| Q5 | SM-2 参数初值（ease/interval）如何标定？是否需要 A/B | 数据 / 工程 | Non-blocking |
| Q6 | CORS 生产环境具体白名单来源？ | 工程 | Non-blocking（M6 前） |

---

## 8. 时间线考量（Timeline）

- **硬依赖**：S1（OCR）依赖 Q3 决策；M4（SM-2）依赖 `ReviewLog` 数据模型与排程服务，是核心价值路径，建议最早联调。
- **分期建议**（对标 `DEV_PLAN.md` M1–M7）：
  - 阶段一（Sprint 1–2）：基础巩固 + 可观测（M1–M6 已落地部分补测试/日志）
  - 阶段二（Sprint 3–5）：**M4 SM-2 复习引擎 + S3 变体/批改**（关键路径）
  - 阶段三（Sprint 5–6）：S1 OCR + S4 看板 + S5 导出
  - 阶段四（Sprint 6–7）：S6 帮助 + 移动端 + 性能/安全 + 发布
- **风险提示**：第三方 LLM 稳定性（R1）、OCR 依赖（R2）为高可能/高影响，需在阶段二前有 fallback 与降级验证。

---

## 附录 A：当前实现状态快照（2026-08-13 代码）

| 能力 | 状态 | 备注 |
|------|------|------|
| 文本/对话录入 + AI 解析 | ✅ 已落地 | 多供应商路由 + fallback 已验证 |
| 图片/OCR 录入 | 🟡 接口通，依赖缺 | 需装 PaddleOCR 或换方案 |
| 错题本 CRUD/筛选/分类 | ✅ 已落地 | — |
| 逐题复习（标记） | 🟡 部分 | 仅标记，无 SM-2 排程队列/到期日 |
| AI 对话（流式） | ✅ 已落地 | + 转错题 |
| 数据看板 / 帮助 | ✅ 视图已落地 | 可视化/内容待增强 |
| PDF 导出 | ✅ 已落地 | ReportLab 中文 |
| 变体题/批改/相似题 | 🟡 接口通，未接入前端流 | `/ai/variant`、`/ai/grade`、`/analysis/similar` 存在 |
| Markdown 导出 | ❌ 未做 | S5 |
| 测试覆盖 | ❌ 薄弱 | 需补单测/集成/E2E |

> 本 PRD 与 `DEV_PLAN.md`、`PRD_UIUX.md` 配套；状态判定以代码快照为准，随开发推进在 DEV_PLAN 更新。
